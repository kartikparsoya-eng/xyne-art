# --- Secrets / credentials — NEVER commit ---
# minted JWTs (real signed tokens)
harness/auth-pool.json
*.jwt
.env
.env.*

# --- Real data / PII-adjacent — do not commit ---
# entity ID pools: PROD (gen_id_pool.py), sandbox/trace (gen_id_pool_db.py) —
# every real pool variant; only the example ships
harness/id-pool*.json
!harness/id-pool.example.json
# raw telemetry pulls (log samples contain user emails)
raw/

# --- Generated / environment-specific — regenerable per rig ---
# clientSchemas: harvested (run-art-local.sh --refresh), fitted
# (fit_remote_schema.py), and per-backend-commit snapshots — all of them
harness/client-schema*.json
# run/gate reports, logs, blessed local baselines, heap snapshots
reports/

# --- Local tooling ---
.venv/
__pycache__/
*.pyc
.DS_Store
